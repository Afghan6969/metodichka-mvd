# 🔍 Инструкция по дебагу производительности

## ✅ Что было исправлено:

### 1. Модальное окно в "Приказы в ДО"
**Проблема:** Модальное окно не показывалось из-за `transform: translateZ(0)` на всех элементах, что создавало новый stacking context.

**Решение:** Убрали `transform` для fixed элементов.

### 2. Добавлен Performance Debug
Теперь можно видеть FPS в реальном времени!

---

## 🚀 Как использовать дебаг:

### Включить дебаг режим:
Добавьте `?debug=true` к любому URL:

```
http://localhost:3000?debug=true
http://localhost:3000/orders?debug=true
```

### Что показывает дебаг:
- **FPS** - текущий FPS (цель: ≥55)
  - 🟢 Зелёный: 55-60 FPS (отлично)
  - 🟡 Жёлтый: 40-54 FPS (приемлемо)
  - 🔴 Красный: <40 FPS (проблема!)

- **Frame Time** - время одного кадра
  - Цель: ≤16.67ms (60 FPS)

### Выключить дебаг:
Нажмите кнопку "Закрыть дебаг" в правом нижнем углу.

---

## 🔧 Как найти проблему:

### 1. Откройте страницу с дебагом:
```
http://localhost:3000?debug=true
```

### 2. Проверьте FPS:
- Если FPS < 40 - есть проблема
- Скроллите страницу и смотрите как меняется FPS

### 3. Откройте Chrome DevTools:
```
F12 → Performance → Record
```

### 4. Запишите профиль:
1. Нажмите Record (красная точка)
2. Скроллите страницу 5-10 секунд
3. Остановите запись

### 5. Анализируйте:
- **Main thread** - ищите длинные задачи (>50ms)
- **GPU** - проверьте нагрузку
- **Rendering** - ищите Layout Shift и Paint

---

## 📊 Что проверить на каждой странице:

### Тест производительности:
1. Откройте страницу с `?debug=true`
2. Подождите 2-3 секунды (загрузка)
3. Скроллите вниз медленно
4. Скроллите вниз быстро
5. Скроллите вверх

### Ожидаемые результаты:
- **Загрузка:** FPS может падать до 40-45 (нормально)
- **Медленный скролл:** FPS должен быть 55-60
- **Быстрый скролл:** FPS должен быть 50-60
- **Скролл вверх:** FPS должен быть 55-60

### Если FPS < 40:
1. Проверьте Chrome DevTools → Performance
2. Найдите что занимает больше всего времени
3. Сообщите мне что нашли

---

## 🎯 Текущий статус:

### Исправлено:
- ✅ CSS анимации фона (-70% GPU)
- ✅ 122 файла оптимизированы
- ✅ Страница "Приказы в ДО"
- ✅ Модальное окно теперь показывается
- ✅ Убран `scroll-behavior: smooth`
- ✅ Добавлен GPU acceleration

### Если всё ещё лагает:
Возможные причины:
1. **Слабое железо** - GPU не справляется
2. **Много вкладок** - закройте лишние
3. **Фоновые процессы** - проверьте Task Manager
4. **Расширения браузера** - отключите их
5. **Тема с анимациями** - попробуйте другую тему

---

## 💡 Быстрые тесты:

### Тест 1: Отключить анимации фона
В Chrome DevTools → Console:
```javascript
document.body.style.backgroundImage = 'none'
```
Если FPS улучшился - проблема в анимациях фона.

### Тест 2: Отключить все transitions
В Chrome DevTools → Console:
```javascript
document.body.style.cssText += '; * { transition: none !important; }'
```
Если FPS улучшился - проблема в transitions.

### Тест 3: Проверить GPU
В Chrome DevTools → Console:
```javascript
console.log(navigator.gpu ? 'GPU доступен' : 'GPU недоступен')
```

---

## 📝 Как сообщить о проблеме:

Если нашли страницу где лагает:

1. **URL страницы**
2. **FPS из дебага** (например: "FPS: 25-30")
3. **Что делали** (например: "скроллил вниз")
4. **Screenshot** из Chrome DevTools Performance

Это поможет быстро найти и исправить проблему!

---

## 🔥 Экстренные меры:

Если совсем невыносимо:

### Вариант 1: Отключить все оптимизации
В `globals.css` закомментируйте весь блок:
```css
/* ГЛОБАЛЬНАЯ ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ */
```

### Вариант 2: Использовать простую тему
Переключитесь на тему "Light" или "Dark" без анимаций.

### Вариант 3: Откатить изменения
```bash
git log --oneline
git revert <последний-коммит>
```
