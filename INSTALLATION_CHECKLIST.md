# ✅ Чеклист установки системы запросов на создание аккаунта

## 📋 Перед началом

- [ ] У вас есть доступ к Supabase Dashboard
- [ ] У вас есть права на выполнение SQL запросов
- [ ] Проект запущен локально (`npm run dev`)
- [ ] У вас есть тестовый аккаунт с ролью "Лидер ПГС" или "ГС"

## 🗄️ Шаг 1: База данных

### 1.1 Выполнение SQL миграции

- [ ] Открыт Supabase Dashboard
- [ ] Перешли в SQL Editor
- [ ] Открыли файл `scripts/008_create_account_requests_table.sql`
- [ ] Скопировали весь SQL код
- [ ] Вставили в SQL Editor
- [ ] Нажали "Run" или Ctrl+Enter
- [ ] Миграция выполнена без ошибок

### 1.2 Проверка таблиц

Выполните в SQL Editor:
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('account_requests', 'request_rate_limits');
```

- [ ] Таблица `account_requests` создана
- [ ] Таблица `request_rate_limits` создана

### 1.3 Проверка функций

Выполните в SQL Editor:
```sql
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'public' 
AND routine_name IN ('check_rate_limit', 'auto_create_user_from_request');
```

- [ ] Функция `check_rate_limit` создана
- [ ] Функция `auto_create_user_from_request` создана

### 1.4 Проверка триггера

Выполните в SQL Editor:
```sql
SELECT tgname 
FROM pg_trigger 
WHERE tgname = 'trigger_auto_create_user_from_request';
```

- [ ] Триггер `trigger_auto_create_user_from_request` создан

### 1.5 Проверка RLS политик

Выполните в SQL Editor:
```sql
SELECT * FROM pg_policies WHERE tablename = 'account_requests';
```

- [ ] Политика "Anyone can create account requests" существует
- [ ] Политика "Leaders can view account requests" существует
- [ ] Политика "Leaders can update account requests" существует

## 📁 Шаг 2: Файлы проекта

### 2.1 Backend файлы

- [ ] `lib/spam-protection.ts` - Утилиты защиты от спама
- [ ] `app/api/account-requests/submit/route.ts` - API отправки запроса
- [ ] `app/api/account-requests/list/route.ts` - API списка запросов
- [ ] `app/api/account-requests/review/route.ts` - API одобрения/отклонения
- [ ] `app/api/account-requests/generate-captcha/route.ts` - API генерации CAPTCHA

### 2.2 Frontend файлы

- [ ] `components/account-request-form.tsx` - Форма запроса
- [ ] `components/account-requests-admin.tsx` - Админ-панель
- [ ] `app/account-request/page.tsx` - Страница формы
- [ ] `app/admin/account-requests/page.tsx` - Админ-страница

### 2.3 Дополнительные файлы

- [ ] `types/account-requests.ts` - TypeScript типы
- [ ] `docs/ACCOUNT_REQUESTS.md` - Полная документация
- [ ] `docs/ACCOUNT_REQUESTS_SETUP.md` - Быстрая установка
- [ ] `docs/ACCOUNT_REQUESTS_FLOW.md` - Диаграммы потоков
- [ ] `ACCOUNT_REQUESTS_SUMMARY.md` - Сводка системы

## 🧪 Шаг 3: Тестирование

### 3.1 Тест: Генерация CAPTCHA

Выполните в терминале:
```bash
curl http://localhost:3000/api/account-requests/generate-captcha
```

- [ ] API возвращает JSON с `question` и `token`
- [ ] Вопрос содержит математическое выражение

### 3.2 Тест: Отправка запроса (публичная форма)

1. Откройте браузер
2. Перейдите на `http://localhost:3000/account-request`

- [ ] Страница загружается без ошибок
- [ ] Форма отображается корректно
- [ ] CAPTCHA загружается автоматически
- [ ] Все поля формы работают

3. Заполните форму:
   - Ник: `TestUser123`
   - Логин: `testuser`
   - Пароль: `password123`
   - Подтверждение: `password123`
   - Роль: `Новичок`
   - CAPTCHA: решите задачу

4. Нажмите "Отправить запрос"

- [ ] Запрос отправлен успешно
- [ ] Показано сообщение об успехе
- [ ] Форма очищена

5. Проверьте в базе данных:
```sql
SELECT * FROM account_requests ORDER BY created_at DESC LIMIT 1;
```

- [ ] Запрос сохранен в БД
- [ ] Статус = `pending`
- [ ] IP адрес записан
- [ ] Пароль захэширован

### 3.3 Тест: Валидация полей

Попробуйте отправить запрос с неверными данными:

- [ ] Пустой ник → показывает ошибку
- [ ] Короткий ник (< 3 символов) → показывает ошибку
- [ ] Короткий логин (< 3 символов) → показывает ошибку
- [ ] Короткий пароль (< 6 символов) → показывает ошибку
- [ ] Несовпадающие пароли → показывает ошибку
- [ ] Пустая CAPTCHA → показывает ошибку
- [ ] Неверная CAPTCHA → показывает ошибку

### 3.4 Тест: Защита от спама

1. Отправьте 3 запроса подряд с разными логинами

- [ ] Первый запрос успешен
- [ ] Второй запрос успешен
- [ ] Третий запрос успешен

2. Попробуйте отправить 4-й запрос

- [ ] Показывает ошибку "Превышен лимит запросов"
- [ ] Статус код 429

3. Проверьте в БД:
```sql
SELECT * FROM request_rate_limits ORDER BY last_request_at DESC LIMIT 1;
```

- [ ] Запись создана для вашего IP
- [ ] `request_count` = 3 или больше
- [ ] `blocked_until` установлен

### 3.5 Тест: Проверка дубликатов

1. Попробуйте отправить запрос с существующим логином

- [ ] Показывает ошибку "Логин уже занят"
- [ ] Статус код 409

### 3.6 Тест: Админ-панель (просмотр)

1. Войдите как пользователь с ролью "Лидер ПГС" или "ГС"
2. Перейдите на `http://localhost:3000/admin/account-requests`

- [ ] Страница загружается без ошибок
- [ ] Отображается список запросов
- [ ] Вкладки работают (Ожидают, Одобренные, Отклоненные, Все)
- [ ] Кнопка "Обновить" работает
- [ ] Пагинация работает (если запросов > 20)

### 3.7 Тест: Одобрение запроса

1. В админ-панели найдите запрос со статусом "Ожидает"
2. Нажмите кнопку "Одобрить"

- [ ] Открывается модальное окно
- [ ] Отображаются детали запроса
- [ ] Поле для комментария работает

3. Добавьте комментарий (опционально) и подтвердите

- [ ] Запрос одобрен успешно
- [ ] Показано уведомление об успехе
- [ ] Список обновлен
- [ ] Статус запроса изменился на "Одобрен"

4. Проверьте в БД:
```sql
-- Проверка запроса
SELECT * FROM account_requests WHERE id = 'REQUEST_ID';

-- Проверка создания пользователя
SELECT * FROM users ORDER BY created_at DESC LIMIT 1;
```

- [ ] Статус запроса = `approved`
- [ ] `reviewed_by` заполнен
- [ ] `reviewed_at` заполнен
- [ ] Комментарий сохранен (если был)
- [ ] Пользователь создан в таблице `users`
- [ ] Данные пользователя совпадают с запросом

5. Проверьте логи:
```sql
SELECT * FROM user_logs 
WHERE action = 'account_request_approved' 
ORDER BY created_at DESC LIMIT 1;
```

- [ ] Действие залогировано
- [ ] `performed_by_id` = ID проверяющего
- [ ] `target_user_nickname` = ник из запроса

### 3.8 Тест: Отклонение запроса

1. В админ-панели найдите запрос со статусом "Ожидает"
2. Нажмите кнопку "Отклонить"
3. Добавьте комментарий (например, "Неверные данные")
4. Подтвердите

- [ ] Запрос отклонен успешно
- [ ] Статус изменился на "Отклонен"
- [ ] Комментарий сохранен
- [ ] Пользователь НЕ создан в таблице `users`

5. Проверьте логи:
```sql
SELECT * FROM user_logs 
WHERE action = 'account_request_rejected' 
ORDER BY created_at DESC LIMIT 1;
```

- [ ] Действие залогировано

### 3.9 Тест: Доступ (безопасность)

1. Выйдите из системы или откройте инкогнито
2. Попробуйте открыть `/admin/account-requests`

- [ ] Редирект на главную страницу или страницу входа
- [ ] Админ-панель недоступна без авторизации

3. Войдите как пользователь с ролью НЕ "Лидер ПГС" и НЕ "ГС"
4. Попробуйте открыть `/admin/account-requests`

- [ ] Редирект на главную страницу
- [ ] Админ-панель недоступна для обычных пользователей

## 🎨 Шаг 4: UI/UX проверка

### 4.1 Форма запроса

- [ ] Дизайн выглядит современно и чисто
- [ ] Все поля выровнены
- [ ] Лейблы читаемы
- [ ] Подсказки под полями понятны
- [ ] Кнопки имеют правильные цвета
- [ ] Индикаторы загрузки работают
- [ ] Сообщения об ошибках понятны
- [ ] Сообщение об успехе выглядит хорошо
- [ ] CAPTCHA легко читается
- [ ] Кнопка "Обновить" CAPTCHA работает

### 4.2 Админ-панель

- [ ] Вкладки переключаются плавно
- [ ] Карточки запросов выглядят аккуратно
- [ ] Бейджи статусов имеют правильные цвета
- [ ] Кнопки одобрения/отклонения различимы
- [ ] Модальное окно центрировано
- [ ] Пагинация работает корректно
- [ ] Кнопка обновления имеет анимацию

## 📱 Шаг 5: Адаптивность (опционально)

### 5.1 Мобильные устройства

Откройте DevTools (F12) и переключитесь на мобильный вид:

- [ ] Форма запроса адаптируется под мобильный экран
- [ ] Админ-панель адаптируется под мобильный экран
- [ ] Все кнопки кликабельны на мобильном
- [ ] Текст читаем на маленьком экране

## 🔧 Шаг 6: Настройка (опционально)

### 6.1 Изменение лимита запросов

Если нужно изменить лимит с 3 до другого значения:

1. Откройте SQL Editor в Supabase
2. Выполните:
```sql
CREATE OR REPLACE FUNCTION check_rate_limit(p_ip_address TEXT)
RETURNS BOOLEAN AS $$
DECLARE
  v_record RECORD;
  v_time_window INTERVAL := '24 hours';  -- Измените здесь
  v_max_requests INTEGER := 5;           -- Измените здесь (например, на 5)
BEGIN
  -- ... остальной код без изменений
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

- [ ] Функция обновлена
- [ ] Новый лимит работает

### 6.2 Добавление ролей

Если нужно добавить новые роли/фракции:

1. Откройте `components/account-request-form.tsx`
2. Найдите `<SelectContent>`
3. Добавьте новые `<SelectItem>`

- [ ] Новые роли добавлены
- [ ] Роли отображаются в выпадающем списке

## 📊 Шаг 7: Мониторинг

### 7.1 Полезные SQL запросы

Сохраните эти запросы для мониторинга:

```sql
-- Статистика по запросам
SELECT 
  status,
  COUNT(*) as count
FROM account_requests
GROUP BY status;

-- Последние 10 запросов
SELECT 
  nickname,
  login,
  status,
  created_at
FROM account_requests
ORDER BY created_at DESC
LIMIT 10;

-- Топ IP по количеству запросов
SELECT 
  ip_address,
  COUNT(*) as request_count
FROM account_requests
GROUP BY ip_address
ORDER BY request_count DESC
LIMIT 10;

-- Заблокированные IP
SELECT 
  ip_address,
  blocked_until,
  request_count
FROM request_rate_limits
WHERE blocked_until > now()
ORDER BY blocked_until DESC;
```

- [ ] Запросы сохранены
- [ ] Запросы работают корректно

## ✅ Финальная проверка

- [ ] Все таблицы созданы
- [ ] Все функции работают
- [ ] Триггер срабатывает
- [ ] RLS политики активны
- [ ] Все файлы на месте
- [ ] Форма запроса работает
- [ ] Админ-панель работает
- [ ] Валидация работает
- [ ] Защита от спама работает
- [ ] Автоматическое создание пользователя работает
- [ ] Логирование работает
- [ ] Безопасность настроена
- [ ] UI выглядит хорошо

## 🎉 Поздравляем!

Если все пункты отмечены, система запросов на создание аккаунта успешно установлена и готова к использованию!

## 📚 Дополнительные ресурсы

- Полная документация: `docs/ACCOUNT_REQUESTS.md`
- Быстрая установка: `docs/ACCOUNT_REQUESTS_SETUP.md`
- Диаграммы потоков: `docs/ACCOUNT_REQUESTS_FLOW.md`
- Сводка системы: `ACCOUNT_REQUESTS_SUMMARY.md`

## 🐛 Если что-то не работает

1. Проверьте логи Supabase
2. Проверьте консоль браузера (F12)
3. Проверьте терминал Next.js
4. Обратитесь к разделу Troubleshooting в документации

---

**Дата установки**: _______________  
**Установил**: _______________  
**Версия**: 1.0.0
