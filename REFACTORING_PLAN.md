# 🎯 План рефакторинга orders-page.tsx

## Текущая ситуация:
- **1070 строк** в одном файле
- Всё в одном компоненте
- Сложно поддерживать

## Цель:
Разбить на **логические модули** по 100-200 строк каждый.

---

## 📁 Новая структура:

```
components/
└── orders/
    ├── orders-data.ts          ✅ СОЗДАН (данные приказов)
    ├── order-card.tsx          ⏳ TODO (карточка приказа)
    ├── order-filters.tsx       ⏳ TODO (поиск + фильтры)
    ├── order-editor-modal.tsx  ⏳ TODO (модальное окно)
    ├── order-form-fields.tsx   ⏳ TODO (поля формы)
    └── index.tsx               ⏳ TODO (главный компонент)
```

---

## 📝 Детальный план:

### 1. ✅ orders-data.ts (~100 строк)
**Содержит:**
- `interface Order`
- `const categories`
- `const orders` (массив всех приказов)

**Статус:** ✅ Создан (нужно скопировать все приказы)

---

### 2. ⏳ order-card.tsx (~70 строк)
**Содержит:**
- Мемоизированный компонент карточки приказа
- Props: `order`, `onEdit`, `getCategoryColor`, `replaceOrderContent`
- Рендер: заголовок, категория, кнопки, контент

**Код:**
```typescript
"use client"

import { memo } from "react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { CopyButton } from "../copy-button"
import { FileText, Edit } from "lucide-react"
import type { Order } from "./orders-data"

interface OrderCardProps {
  order: Order
  onEdit: (order: Order) => void
  getCategoryColor: (category: string) => string
  replaceOrderContent: (content: string) => string
}

export const OrderCard = memo<OrderCardProps>(({ 
  order, 
  onEdit, 
  getCategoryColor, 
  replaceOrderContent 
}) => {
  return (
    <div className="bg-gradient-to-br from-white/10 to-white/5 border border-white/15 rounded-3xl overflow-hidden group hover:from-white/15 hover:to-white/8 hover:border-blue-400/30 transition-colors duration-200">
      {/* ... остальной код карточки */}
    </div>
  )
})
```

---

### 3. ⏳ order-filters.tsx (~100 строк)
**Содержит:**
- Поиск
- Фильтры по категориям
- Счётчик найденных приказов

**Props:**
- `searchQuery`, `setSearchQuery`
- `selectedCategory`, `setSelectedCategory`
- `filteredCount`
- `categories`

---

### 4. ⏳ order-editor-modal.tsx (~400 строк)
**Содержит:**
- Модальное окно редактора
- Все поля формы
- Логика замены плейсхолдеров
- Кнопки "Скопировать" и "Закрыть"

**Props:**
- `editingOrder`
- `onClose`
- `position`, `setPosition`
- `nickname`, `setNickname`
- и т.д.

---

### 5. ⏳ order-form-fields.tsx (~200 строк)
**Содержит:**
- Компоненты полей формы
- `PromotionTypeSelector`
- `BonusSourceSelector`
- `FineRequiredSelector`
- `DisciplineStateInputs`
- `VacationDatePickers`

---

### 6. ⏳ index.tsx (~200 строк)
**Главный компонент - только логика:**
- useState hooks
- useEffect hooks
- useMemo для фильтрации
- useCallback для функций
- Рендер: PageHeader + Filters + List + Modal

---

## 🎯 Преимущества:

### До:
- ❌ 1070 строк в одном файле
- ❌ Сложно найти нужный код
- ❌ Сложно тестировать
- ❌ Долго загружается в редакторе

### После:
- ✅ 6 файлов по 70-200 строк
- ✅ Легко найти нужный модуль
- ✅ Можно тестировать отдельно
- ✅ Быстро открывается
- ✅ Переиспользуемые компоненты

---

## 📊 Размеры файлов:

| Файл | Строк | Описание |
|------|-------|----------|
| `orders-data.ts` | ~100 | Данные |
| `order-card.tsx` | ~70 | Карточка |
| `order-filters.tsx` | ~100 | Фильтры |
| `order-form-fields.tsx` | ~200 | Поля формы |
| `order-editor-modal.tsx` | ~400 | Модалка |
| `index.tsx` | ~200 | Главный |
| **ИТОГО** | **~1070** | **Как было** |

---

## 🚀 Как применить:

### Вариант 1: Я сделаю за вас
Скажите "сделай рефакторинг" и я:
1. Создам все 6 файлов
2. Перенесу код по модулям
3. Обновлю импорты
4. Протестирую что всё работает

### Вариант 2: Постепенно
Делаем по одному файлу за раз:
1. Сначала `order-card.tsx`
2. Потом `order-filters.tsx`
3. И так далее

### Вариант 3: Вы сами
Следуйте этому плану и создавайте файлы по очереди.

---

## 💡 Рекомендация:

**Вариант 1** - самый быстрый и безопасный.
Я сделаю всё аккуратно, без ошибок, с правильными импортами.

Что скажете? 🎯
