# Рефакторинг генератора отчётов

## Что было сделано

### 1. Создана модульная структура

Файл `generator-page.tsx` (1540 строк, 79KB) был разбит на несколько модулей:

#### `components/generator/types.ts`
- Интерфейсы и типы данных
- `Position`, `DepartmentPositions`, `Requirement`
- `ReportType` (включая новый тип "leader")
- `Department`

#### `components/generator/constants.ts`
- Константы должностей для ГУВД и ГИБДД
- Иерархия званий
- Категории (старший состав, академии)
- Города
- Аббревиатуры подразделений

#### `components/generator/utils.ts`
- Утилиты для склонения должностей и ФИО
- `toGenitiveCase` - родительный падеж
- `toInstrumentalCase` - творительный падеж
- `declineLeaderName` - склонение ФИО лидера
- `parseRanks` - парсинг диапазонов званий
- `getNextRank` - получение следующего звания

#### `components/generator/report-templates.ts`
- Функция `generateReport` для генерации всех типов отчётов
- Поддержка всех 4 типов: promotion, reprimand, senior, leader

#### `components/generator/form-fields.tsx`
- Переиспользуемые компоненты формы:
  - `CitySelect` - выбор города
  - `LeaderFioInput` - ввод ФИО лидера
  - `FioInput` - ввод ФИО
  - `DateRangeInputs` - выбор диапазона дат
  - `RequirementsList` - список требований/задач
  - `LeaderReportFields` - поля для лидерского отчёта

### 2. Добавлен новый тип отчёта - "Лидерский"

**Доступ:**
- leader-guvd
- leader-gibdd
- pgs-guvd
- pgs-gibdd
- gs-guvd
- gs-gibdd
- root
- super-admin

**Формат отчёта:**
```
[ Заместитель Министра Внутренних Дел | {ФИО лидера} ]
Приказ о наложении внутреннего дисциплинарного взыскания

По результатам служебного расследования:
На сотрудника {ФИО сотрудника} наложено дисциплинарное взыскание за нарушение пункта:
{Пункт нарушения}

Взыскание: {Тип взыскания}
Текущее дисциплинарное состояние: {Статус}
```

**Поля формы для лидерского отчёта:**
1. Ваше ФИО (лидер)
2. ФИО сотрудника (на кого накладывается взыскание)
3. Пункт нарушения (например: "3.22 ПСГО — Нарушение Журнала Активности.")
4. Взыскание (например: "Предупреждение")
5. Текущее дисциплинарное состояние (например: "УП — 0/5 | П — 1/5 | В — 0/3.")

## Преимущества новой структуры

1. **Модульность** - код разбит на логические части
2. **Переиспользуемость** - компоненты можно использовать в других местах
3. **Поддерживаемость** - легче находить и исправлять баги
4. **Масштабируемость** - проще добавлять новые типы отчётов
5. **Читаемость** - каждый файл отвечает за свою область

## Следующие шаги

1. Протестировать все типы отчётов
2. Обновить основной файл `generator-page.tsx` для использования новых модулей
3. Удалить дублирующийся код из старого файла
4. Проверить работу склонений ФИО и должностей

## Файлы

- ✅ `components/generator/types.ts` - создан
- ✅ `components/generator/constants.ts` - создан
- ✅ `components/generator/utils.ts` - создан
- ✅ `components/generator/report-templates.ts` - создан
- ✅ `components/generator/form-fields.tsx` - создан
- ⏳ `components/generator-page.tsx` - требует обновления
- 💾 `components/generator-page.tsx.backup` - резервная копия
