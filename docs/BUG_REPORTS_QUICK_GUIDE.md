# Баг Репорт - Краткая инструкция

## Что было создано

✅ **SQL миграция**: `scripts/015_create_bug_reports.sql`
- Таблица `bug_reports` с полями для хранения отчетов
- RLS политики для безопасности
- Индексы для быстрого поиска
- Триггеры для автоматического обновления

✅ **TypeScript типы**: `types/bug-reports.ts`
- Типы для работы с отчетами в TypeScript

✅ **API endpoints**:
- `GET /api/bug-reports` - получение списка отчетов
- `POST /api/bug-reports` - создание отчета
- `PATCH /api/bug-reports/[id]` - обновление отчета (super-admin)
- `DELETE /api/bug-reports/[id]` - удаление отчета (super-admin)

✅ **Компонент страницы**: `components/bug-reports-page.tsx`
- Форма создания отчетов
- Таблица с отчетами
- Фильтры (для super-admin)
- Управление статусами и приоритетами (для super-admin)

✅ **Роутинг**: `app/bug-reports/page.tsx`

✅ **Навигация**: Добавлен пункт "Баг репорт" в сайдбар (только для super-admin)

## Что нужно сделать для запуска

### 1. Выполнить SQL миграцию

Откройте Supabase Dashboard → SQL Editor и выполните:

```sql
-- Содержимое файла scripts/015_create_bug_reports.sql
```

### 2. Проверить роль пользователя

Убедитесь, что ваш пользователь имеет роль `super-admin`:

```sql
SELECT email, role FROM users WHERE email = 'ваш-email@example.com';
```

Если нет, обновите:

```sql
UPDATE users SET role = 'super-admin' WHERE email = 'ваш-email@example.com';
```

### 3. Перезапустить приложение

```bash
npm run dev
```

## Как использовать

### Для обычных пользователей

1. Войдите в систему
2. Откройте страницу через прямую ссылку `/bug-reports`
3. Нажмите "Создать отчет"
4. Заполните форму и отправьте

### Для Super-Admin

1. Войдите как super-admin
2. В боковом меню появится пункт "Баг репорт"
3. Откройте страницу
4. Вы увидите все отчеты от всех пользователей
5. Используйте фильтры для поиска
6. Изменяйте статусы и приоритеты
7. Удаляйте отчеты при необходимости

## Особенности

- **Безопасность**: RLS политики защищают данные
- **Валидация**: Минимум 5 символов для заголовка, 10 для описания
- **Автоматизация**: Статус "Решен" автоматически сохраняет дату и автора решения
- **Фильтрация**: Super-admin может фильтровать по типу и статусу
- **Приоритеты**: 4 уровня приоритета (низкий, средний, высокий, критический)

## Типы отчетов

- **Баг** 🐛 - сообщение об ошибке
- **Предложение** 💡 - идея по улучшению

## Статусы

- **Новый** - только что создан
- **В работе** - принят в работу
- **Решен** - проблема решена
- **Отклонен** - отчет отклонен

## Приоритеты

- **Низкий** - незначительная проблема
- **Средний** - обычная проблема
- **Высокий** - важная проблема
- **Критический** - критическая проблема, требует немедленного решения

## Документация

Подробная документация находится в:
- `docs/BUG_REPORTS.md` - полное описание системы
- `docs/BUG_REPORTS_SETUP.md` - инструкция по установке и устранению проблем
