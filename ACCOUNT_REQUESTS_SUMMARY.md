# 📋 Система запросов на создание аккаунта - Сводка

## ✅ Что было создано

### 1. База данных (SQL)
- **`scripts/008_create_account_requests_table.sql`** - Полная миграция базы данных
  - Таблица `account_requests` для хранения запросов
  - Таблица `request_rate_limits` для защиты от спама
  - Функция `check_rate_limit()` для проверки лимитов
  - Функция `auto_create_user_from_request()` для автоматического создания пользователей
  - Триггер для автоматического создания пользователя при одобрении
  - RLS политики для безопасности

### 2. Backend (API Routes)
- **`app/api/account-requests/submit/route.ts`** - Отправка запроса на создание аккаунта
- **`app/api/account-requests/list/route.ts`** - Получение списка запросов (только для лидеров)
- **`app/api/account-requests/review/route.ts`** - Одобрение/отклонение запросов
- **`app/api/account-requests/generate-captcha/route.ts`** - Генерация CAPTCHA

### 3. Утилиты
- **`lib/spam-protection.ts`** - Функции защиты от спама:
  - `getClientIp()` - Получение IP адреса
  - `checkRateLimit()` - Проверка лимита запросов
  - `checkPendingRequest()` - Проверка существующих запросов
  - `checkLoginExists()` - Проверка дубликатов логина
  - `validateRequestFields()` - Валидация полей
  - `verifyCaptcha()` - Проверка CAPTCHA

### 4. Frontend (Components)
- **`components/account-request-form.tsx`** - Форма для отправки запроса
  - Валидация в реальном времени
  - CAPTCHA защита
  - Красивый UI с обратной связью
  
- **`components/account-requests-admin.tsx`** - Админ-панель для лидеров
  - Просмотр всех запросов
  - Фильтрация по статусу
  - Одобрение/отклонение с комментариями
  - Пагинация

### 5. Pages
- **`app/account-request/page.tsx`** - Публичная страница для отправки запросов
- **`app/admin/account-requests/page.tsx`** - Админ-страница для лидеров ПГС и ГС

### 6. TypeScript Types
- **`types/account-requests.ts`** - Типы для TypeScript

### 7. Документация
- **`docs/ACCOUNT_REQUESTS.md`** - Полная документация системы
- **`docs/ACCOUNT_REQUESTS_SETUP.md`** - Быстрая инструкция по установке

## 🎯 Основные возможности

### Для игроков:
✅ Отправка запроса на создание аккаунта  
✅ Валидация полей в реальном времени  
✅ CAPTCHA защита от ботов  
✅ Уведомление об успешной отправке  

### Для лидеров ПГС и ГС:
✅ Просмотр всех запросов  
✅ Фильтрация по статусу (ожидают, одобренные, отклоненные)  
✅ Одобрение запросов с автоматическим созданием аккаунта  
✅ Отклонение запросов с комментариями  
✅ Логирование всех действий  

### Защита от спама:
✅ Rate limiting: максимум 3 запроса с одного IP за 24 часа  
✅ CAPTCHA для защиты от ботов  
✅ Проверка дубликатов логинов  
✅ Валидация всех полей  
✅ IP логирование  
✅ Блокировка IP при превышении лимита  

## 🚀 Как установить

### Шаг 1: Выполните SQL миграцию
```sql
-- Откройте Supabase SQL Editor
-- Выполните файл: scripts/008_create_account_requests_table.sql
```

### Шаг 2: Проверьте создание таблиц
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('account_requests', 'request_rate_limits');
```

### Шаг 3: Готово!
Система готова к использованию:
- Игроки: `/account-request`
- Админы: `/admin/account-requests`

## 📊 Структура данных

### Таблица `account_requests`
```
id              UUID (PK)
nickname        TEXT
login           TEXT (UNIQUE)
password_hash   TEXT
role            TEXT (nullable)
status          TEXT (pending/approved/rejected)
comment         TEXT (nullable)
created_at      TIMESTAMP
reviewed_by     UUID (FK -> users.id)
reviewed_at     TIMESTAMP (nullable)
ip_address      TEXT
user_agent      TEXT
```

### Таблица `request_rate_limits`
```
id                UUID (PK)
ip_address        TEXT
request_count     INTEGER
last_request_at   TIMESTAMP
blocked_until     TIMESTAMP (nullable)
```

## 🔒 Безопасность

### Хэширование паролей
- Используется bcrypt с 10 раундами
- Пароли никогда не хранятся в открытом виде

### Row Level Security (RLS)
- Любой может создавать запросы
- Только лидеры ПГС и ГС могут просматривать и обновлять
- Rate limits доступны только для service role

### Аутентификация
- JWT токены в httpOnly cookies
- Проверка роли и статуса пользователя
- Защита от CSRF

## 📈 Валидация полей

### Ник (nickname)
- Минимум 3 символа, максимум 50
- Только буквы, цифры, пробелы, дефисы, подчеркивания
- Regex: `^[a-zA-Zа-яА-ЯёЁ0-9_\-\s]+$`

### Логин (login)
- Минимум 3 символа, максимум 30
- Только латинские буквы, цифры, дефисы, подчеркивания
- Regex: `^[a-zA-Z0-9_\-]+$`
- Автоматически в нижнем регистре

### Пароль (password)
- Минимум 6 символов, максимум 100
- Хэшируется перед сохранением

## 🔄 Автоматическое создание пользователя

При одобрении запроса:
1. Триггер `trigger_auto_create_user_from_request` срабатывает автоматически
2. Создается новая запись в таблице `users`
3. Устанавливается `created_by` = ID проверяющего
4. Логируется действие в `user_logs`

## 📝 API Endpoints

### POST `/api/account-requests/submit`
Отправка запроса на создание аккаунта

### GET `/api/account-requests/list`
Получение списка запросов (только для лидеров)

### POST `/api/account-requests/review`
Одобрение/отклонение запроса (только для лидеров)

### GET `/api/account-requests/generate-captcha`
Генерация CAPTCHA

## 🎨 UI/UX

### Форма запроса
- Современный дизайн с использованием shadcn/ui
- Валидация в реальном времени
- Понятные сообщения об ошибках
- CAPTCHA с возможностью обновления
- Индикаторы загрузки
- Успешное уведомление после отправки

### Админ-панель
- Вкладки для фильтрации по статусу
- Карточки с детальной информацией
- Кнопки одобрения/отклонения
- Модальное окно для подтверждения действий
- Пагинация для больших списков
- Кнопка обновления данных

## 🛠️ Настройка

### Изменение лимита запросов
Отредактируйте функцию `check_rate_limit` в SQL:
```sql
v_time_window INTERVAL := '24 hours';  -- Временное окно
v_max_requests INTEGER := 3;           -- Максимум запросов
```

### Добавление ролей/фракций
Отредактируйте `components/account-request-form.tsx`:
```tsx
<SelectItem value="Новая роль">Новая роль</SelectItem>
```

### Улучшение CAPTCHA
Замените на Google reCAPTCHA v3 для production

## 📚 Документация

- **Полная документация**: `docs/ACCOUNT_REQUESTS.md`
- **Быстрая установка**: `docs/ACCOUNT_REQUESTS_SETUP.md`
- **Типы TypeScript**: `types/account-requests.ts`

## ✨ Дополнительные возможности

### Можно добавить в будущем:
- Email уведомления при изменении статуса
- Telegram уведомления
- Расширенная аналитика и дашборд
- Экспорт данных в CSV/Excel
- Массовое одобрение/отклонение
- История изменений запроса
- Прикрепление документов к запросу
- Google reCAPTCHA v3 вместо простой CAPTCHA

## 🧪 Тестирование

### Тест 1: Отправка запроса
1. Откройте `/account-request`
2. Заполните форму
3. Решите CAPTCHA
4. Отправьте запрос

### Тест 2: Просмотр запросов
1. Войдите как лидер ПГС или ГС
2. Откройте `/admin/account-requests`
3. Проверьте отображение запросов

### Тест 3: Одобрение запроса
1. Нажмите "Одобрить"
2. Добавьте комментарий
3. Подтвердите
4. Проверьте создание пользователя в БД

### Тест 4: Защита от спама
1. Отправьте 3 запроса подряд
2. Попробуйте отправить 4-й
3. Должна появиться ошибка

## 🐛 Troubleshooting

### Проблема: Триггер не создает пользователя
**Решение**: Проверьте логи Supabase и права функции

### Проблема: RLS блокирует запросы
**Решение**: Проверьте политики RLS

### Проблема: CAPTCHA не загружается
**Решение**: Проверьте работу API route

## 📞 Поддержка

Полная документация доступна в:
- `docs/ACCOUNT_REQUESTS.md`
- `docs/ACCOUNT_REQUESTS_SETUP.md`

---

**Система готова к использованию! 🎉**

Все файлы созданы, документация написана, защита от спама настроена.
